apiVersion: v1
kind: Service
metadata:
  name: tak-database
  namespace: tak-server
  labels:
    app: takserver-db
spec:
  selector:
    app: takserver-db
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP
