apiVersion: v1
kind: Secret
metadata:
  name: authentik-secrets
  namespace: authentik
type: Opaque
stringData:
  # IMPORTANT: Change these values before deploying!
  # For production, use AKV SecretProviderClass instead.
  POSTGRES_PASSWORD: "changeme-authentik-pg"
  SECRET_KEY: "changeme-authentik-secret-key-must-be-long-and-random"
